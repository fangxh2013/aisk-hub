# Public publication policy

公开面只导出可复用内核与适配接口。`public scan` 默认阻断：历史绝对路径、私有家目录、内网地址和配置文件中的疑似凭据字面量。`public verify` 只扫描公开 `master` 与显式公开 Tag 的完整历史；本地任务分支属于运行态，不进入公开导出。

内部迁移计划和历史环境记录保存在本地用于审计，不作为公开包组成部分。公开包不得反向引用它们，也不得通过 Git submodule、远程 URL 或软链接获取运行时内核。
