# 任务入口：{{task_id}} {{title}}

1. 先完整阅读 `AGENT-WORKTREE.md`（任务须知，优先级最高），按其中步骤 `aisk task claim` 认领，再读 `TASK.md` 与 `PROGRESS.md`。
2. 改某个仓库前，先读该仓库根目录的 AGENTS.md（{{repo_agents_list}}）；与任务须知冲突时，以任务须知为准。
3. 过程中用 `{{cli}} note {{task_id}}` 记进度；完成后填 `HANDOFF.md` 并执行 `{{cli}} ready {{task_id}}`。
