# Task workflow

1. 查找相同目标的任务；存在则继续，不重复创建。
2. 认领后确认范围、基线、验收、禁止操作、工作区路径与 `workspace_mode`。direct 仓库必须确认持久单写者租约。
3. 小步记录进度，避免把未验证假设写成完成结果。
4. 检查、交接、落地和上游发布按当前 profile 分阶段执行；不得把一个仓库或分支的自动授权扩展到其他目标。direct 仓库按其专用 finish 路径提交，不能创建任务 worktree。
5. 发生冲突时比较共同祖先、两侧变化和业务意图，逐块处理并重新验证。
